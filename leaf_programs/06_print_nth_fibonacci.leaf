const nth_fibonacci = function (num) {
    if (num == 1) {
        return 0;
    } elseif (num == 2) {
        return 1;
    }
    return nth_fibonacci(num - 1) + nth_fibonacci(num - 2);
};

for var num = 1; num <= 21; num = num + 1 {
    println nth_fibonacci(num);
}
