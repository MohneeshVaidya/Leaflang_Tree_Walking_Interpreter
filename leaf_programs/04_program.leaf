const binaryOperation = function (operation, a, b) {
    return operation(a, b);
};

const addNums = function (a, b) {
    return a + b;
};
const operator = addNums;

println operator(100, 101);
println binaryOperation(addNums, 100, 101);
